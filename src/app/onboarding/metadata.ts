import { Metadata } from 'next';
import { generatePageMetadata } from '../metadata';

export const metadata: Metadata = generatePageMetadata(
  'Onboarding Process | Getting Started',
  'Learn about our simple onboarding process and the client journey at Lex Consulting, from your first discovery call to ongoing structured education sessions.',
  '/onboarding'
); 