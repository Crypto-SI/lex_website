import { Metadata } from 'next';
import { generatePageMetadata } from '../metadata';

export const metadata: Metadata = generatePageMetadata(
  'Contact Us | Get in Touch',
  'Reach out to Lex Consulting to schedule a discovery call, discuss our services, or inquire about personalized financial education solutions.',
  '/contact'
); 